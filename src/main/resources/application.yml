spring:
   application:
     name: scalesamparkapp
   jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
   datasource:
     driverClassName: com.mysql.jdbc.Driver
     url: jdbc:mysql://localhost:3306/scalesampark
     username: root
     password: root
   
server:
   port: 8090
   servlet:    context-path: /${spring.application.name}
   
secretKey: HSHACCSTFSJBCS